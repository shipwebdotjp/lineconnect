import * as React from 'react';
import ReactDOM from 'react-dom';
import RichmenuForm from './component/RichmenuForm'; //拡張子は省略可能
import "./index.css";

ReactDOM.render(
    <RichmenuForm />,
    document.getElementById('line_richmenu_root')
);
import * as React from 'react';
import ReactDOM from 'react-dom';
import RichmenuIndex from './component/RichmenuIndex'; //拡張子は省略可能
import "./index.css";

ReactDOM.render(
    <RichmenuIndex />,
    document.getElementById('line_richmenu_root')
);